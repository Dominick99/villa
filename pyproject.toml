[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "villa"
version = "0.1.0"
description = "Language-learning immersion simulator"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "langchain-openai",
    "python-dotenv>=1.2.2",
]
